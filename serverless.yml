service: ex-invoicer-services
frameworkVersion: '3'

plugins:
  - serverless-offline

provider:
  name: aws
  runtime: nodejs20.x
  timeout: 120

functions:
  main:
    handler: build/index.handler
    events:
      - http:
          method: any
          path: /{proxy+}
    environment:
      MONGO_URL: mongodb+srv://sathishkumar95:jkUuur4hSvOWxxkE@cluster0.6o46gcp.mongodb.net/?retryWrites=true&w=majority